/*
.ready() executes ONLY after DOM is fully loaded
*/
$(document).ready(function() {
    $("h3").css("text-decoration", "underline"); // underlines all <h2> elements
    $("ul").css("border", "solid 1px #ccc") // adds a border to all lists (ul)
});